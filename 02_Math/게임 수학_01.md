---
title: "게임 수학 - 수와 집합, 함수, 좌표계"
date: 2026-02-02
categories: [카테고리/02_Math]
tags: [Math, Study, Unreal Engine, C++]
description: "집합, 수의 체계, 연산, 함수, 좌표계와 언리얼 엔진에서의 활용"
---

# 📝 [기초 수학: 집합부터 좌표계까지]

## 📅 공부한 날짜
*   **날짜**: 2026-02-02

---

## 🚀 1. 학습 목표 (Objective)
*   수와 집합의 개념을 이해하고 언리얼 엔진의 컨테이너(`TSet`, `TArray`)와 연결한다.
*   함수의 종류와 합성/역함수 개념을 익히고 게임 로직(좌표 변환, 피킹)에 적용한다.
*   공간좌표계와 벡터의 개념을 잡는다.

## 💡 2. 핵심 개념 및 상세 내용 (Key Concepts & Details)

### 2.1 수와 집합 (Numbers and Sets)

#### 1. 집합의 개념
우리가 흔히 "모임"이라고 부르는 것을 수학적으로 명확하게 "집합"이라고 정의함. 
- **소박한 집합론 (Naive Set Theory)**: 명확한 기준만 있다면 어떤 것이든 모아놓은 것을 의미함.
- **프로그래밍 관점**: 데이터를 담는 **컨테이너(Container)**로 이해하면 쉬움.

#### [언리얼 엔진 & C++ 심화 비교]
게임 개발에서는 단순히 수학적인 집합뿐만 아니라, 상황에 맞는 자료구조(Container)를 선택하는 게 매우 중요함.

1.  **`std::set` (C++) / `TSet` (Unreal)**
    *   **특징**: 수학적 집합과 가장 유사함. **순서가 없고, 중복을 허용하지 않음**.
    *   **언제 쓰나?**: 
        *   "현재 던전에 입장한 플레이어 목록" (같은 사람이 두 번 들어올 수 없음).
        *   "이미 획득한 업적 목록" (중복 획득 불가).
    *   **장점**: 내부에 데이터가 있는지 검사하는 속도(`Contains`)가 매우 빠름. (해시 테이블 기반)

2.  **`std::vector` (C++) / `TArray` (Unreal)**
    *   **특징**: **순서가 있고, 중복이 허용됨**. 수학적 집합보다는 '수열'이나 '리스트'에 가까움.
    *   **언제 쓰나?**: 
        *   "인벤토리의 아이템 슬롯" (포션을 여러 칸에 나눠 가질 수 있음, 정렬 순서 중요).
        *   대부분의 게임 로직은 가장 빠르고 가벼운 `TArray`를 기본으로 사용함.

3.  **`std::map` (C++) / `TMap` (Unreal)**
    *   **특징**: **Key-Value 쌍**으로 이루어진 집합.
    *   **언제 쓰나?**: 
        *   "아이템 ID(Key)로 아이템 정보(Value) 찾기".

> **결론**: 무작정 모아둔다고 다 같은 집합이 아님. "중복을 허락할 거냐?", "순서가 필요하냐?", "검색이 빨라야 하냐?"에 따라 `TSet`, `TArray`, `TMap`을 골라 쓰는 게 좋다.

---

### 2. 수의 체계 (Number Systems)
현실의 수는 무한하지만, 컴퓨터는 유한한 메모리를 쓰기 때문에 수를 표현하는 방식(Data Type)이 정해져 있음. 이걸 잘 골라야 최적화가 됨.

#### 1) 정수 (Integers) - 딱 떨어지는 수
개수를 세거나 인덱스(순번)를 매길 때 사용함.
*   **수학**: 자연수($\mathbb{N}$), 정수($\mathbb{Z}$).
*   **언리얼/C++**:
    *   `uint8` (0~255): 매우 작은 수. 아이템 개수나 레벨, 혹은 `enum` 타입에 씀. (네트워크 패킷 줄일 때 필수)
    *   `int32` (약 -21억 ~ 21억): **가장 기본**. 언리얼의 모든 로직은 기본적으로 `int32`를 씀.
    *   `int64`: 아주 큰 수. 리니지 같은 RPG의 경험치나 국가 예산 같은 걸 다룰 때 씀.

#### 2) 실수 (Real Numbers) - 연속된 수
체력, 마나, 위치, 회전, 속도 등 게임의 **거의 모든 물리량**은 실수임.
*   **수학**: 유리수($\mathbb{Q}$), 실수($\mathbb{R}$).
*   **언리얼/C++**:
    *   `float` (32비트): 전통적인 게임 실수형. 소수점 아래 정밀도가 제한적임.
    *   `double` (64비트): **언리얼 5의 핵심 변경점 (LWC)**.
        *   **LWC (Large World Coordinate)**: 예전엔 `float`의 한계로 맵이 너무 커지면(예: 20km 이상) 외곽에서 덜덜 떨리는 버그가 있었음. 언리얼 5부터는 기본 자료형을 `double`로 바꿔서 지구 크기만 한 맵도 오차 없이 만들 수 있게 됨.

#### 3) 사원수 (Quaternions) - 복소수의 확장
*   **수학**: 실수 1개 + 허수 3개 ($w + xi + yj + zk$). 4차원 수임.
*   **왜 씀?**: 3차원 공간에서 물체를 **회전**시킬 때 씀.
    *   오일러 각(x, y, z축 회전)은 **짐벌 락(Gimbal Lock)**이라는 치명적인 문제(축이 겹쳐서 회전이 먹통 되는 현상)가 있음.
    *   쿼터니언(`FQuat`)을 쓰면 이 문제가 싹 사라지고, 회전끼리 보간(Lerp)하기도 아주 좋음.
    *   **언리얼**: `FRotator`(오일러)를 에디터에서 주로 보지만, 내부 엔진 연산은 다 `FQuat`(사원수)로 변환해서 처리함.

---

### 2.2 연산과 수의 구조 (Operations and Structure)

#### 1. 닫혀 있다 (Closure)
수학에서 "닫혀 있다"는 건, **연산을 하고 나서도 여전히 그 집합 안에 있다**는 뜻임.
*   **벡터 공간**: 벡터끼리 더하거나 빼면 결과도 무조건 벡터임. -> **"벡터 덧셈에 대해 닫혀 있다."**
*   **내적(Dot Product)**: 벡터 두 개를 내적하면 결과가 벡터가 아니라 **실수(Scalar)**가 됨. -> **"벡터 내적에 대해 닫혀 있지 않다."**
*   이 개념을 알면 함수가 뭘 리턴할지 예측할 수 있음. (`Vector + Vector = Vector`, `Vector dot Vector = float`)

#### 2. 항등원과 역원 (Identity & Inverse)
게임 로직 짤 때 **"초기화"**랑 **"되돌리기"**

**항등원 (Identity)**: 연산을 해도 **변화가 없는** 상태를 만드는 수.
*   **덧셈**: `0` ($A + 0 = A$) -> 위치 이동 없음.
*   **곱셈**: `1` ($A \times 1 = A$) -> 크기 변화 없음.
*   **행렬**: `FMatrix::Identity` -> 아무런 변환도 안 함.
*   **회전**: `FQuat::Identity` -> 회전 안 함 (0도). 

**역원 (Inverse)**: 연산 결과를 다시 **항등원으로 되돌리는** 수. 즉, 했던 짓을 **취소(Undo)**하는 것임.
*   **덧셈**: `-A` (5만큼 갔으면 -5만큼 가야 원점).
*   **행렬**: `Inverse()` (역행렬).
    *   *활용*: **월드 좌표를 로컬 좌표로 바꿀 때** 씀. 
    *   언리얼 함수: `UKismetMathLibrary::InverseTransformLocation()`

---

### 2.3 함수 (Functions)

#### 1. 함수란? (입력과 출력의 관계)
프로그래머한테 함수는 "코드를 묶어놓은 것"이지만, 수학적 함수는 **"입력을 넣으면 정해진 출력이 나오는 자판기"**임.

**블루프린트에서의 함수 구분**:
1.  **Pure Function (순수 함수)**:
    *   실행 핀이 없음. 입력 넣으면 계산해서 결과만 뱉음.
    *   게임 상태를 바꾸지 않음. (예: 덧셈 노드, `GetActorLocation`).
2.  **Impure Function (비순수 함수)**:
    *   실행 핀이 있음.
    *   **부작용(Side Effect)**이 있음. 게임 속 무언가를 **변하시킴**. (예: `SetActorLocation`).

#### 2. 합성 함수 (Composite Function)
함수 뒤에 함수를 연결하는 것. $f(g(x))$.
*   **그래픽스 파이프라인**: 
    *   `로컬 변환 -> 월드 변환 -> 카메라 변환 -> 투영 변환`이라는 4단계 함수를 연속으로 통과해야 함.
    *   **행렬 곱셈**으로 처리함. 즉, 행렬 곱셈이 곧 합성 함수임.

#### 3. 역함수 (Inverse Function)
결과를 보고 원인을 찾는 것. 거꾸로 추적하기.
*   **마우스 피킹 (Raycasting)**:
    *   마우스는 2D 화면(Result) 위의 점임. 이걸로 3D 월드(Input)의 어디를 찍었는지 알아내야 함.
    *   3D -> 2D로 가는 투영(Projection)의 반대 과정이라 **Deprojection**이라고 함.
    *   언리얼 함수: `PC->DeprojectScreenPositionToWorld()`.

---

### 2.4 곱집합과 좌표계 (Cartesian Product)

#### 1. 곱집합
집합 두 개를 곱해서($A \times B$) 짝을 짓는 것. 
실수 $\mathbb{R}$을 두 번 곱하면 $\mathbb{R}^2$ (2차원), 세 번 곱하면 $\mathbb{R}^3$ (3차원)이 됨.

#### 2. 우리가 쓰는 좌표계 (Dimension)
게임에서는 차원별로 용도가 명확함.

*   **1차원 ($\mathbb{R}^1$)**: **스칼라(Scalar)**. 체력, 경험치, 속력.
*   **2차원 ($\mathbb{R}^2$)**: **`FVector2D`**. UI 좌표, UV 좌표.
*   **3차원 ($\mathbb{R}^3$)**: **`FVector`**. 월드 좌표.
    *   **언리얼의 특징**: **왼손 좌표계 + Z-up** (앞쪽:+X, 오른쪽:+Y, 위쪽:+Z).
*   **4차원 ($\mathbb{R}^4$)**: **`FVector4`**. 색상(RGBA), 동차 좌표계.

## ⚠️ 4. 문제 해결 및 시행착오 (Issues & Solutions)
*   **배운 점**: 단순한 수학 공식이 아니라 `TSet` vs `TArray`, `Pure` vs `Impure` 함수 등 엔진 내부 구현 원리와 연결된다는 점을 깨달음.
*   **주의할 점**: 언리얼은 Z축이 위쪽(Up vector)이므로, 다른 툴(Blender, Unity)에서 에셋을 가져올 때 축 변환에 유의해야 함.

## 🔗 5. 참고 자료 (References)
*   [Epic Games: 좌표계 설명](https://dev.epicgames.com/documentation/ko-kr/unreal-engine/coordinate-space-terminology-in-unreal-engine)
*   [득슈의 게임 수학 (유튜브)](https://www.youtube.com/results?search_query=게임수학)
*   [Unreal Docs: TSet](https://dev.epicgames.com/documentation/ko-kr/unreal-engine/set-containers-in-unreal-engine)
*   [이득우의 게임 수학(책)-이득우]
